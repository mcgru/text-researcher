Сделай функцию rm, аналогичную rm_file, только сделай вариадические аргументы, и чтобы среди аргументов обрабатывался кейс '-r' (рекурсивно для директорий), '-f' (форсированно) и
  '-q' (тихо, без вывода в stderr). Сделай rmdir, аналогичную rm_dir, но с вариадическими аргументами и так же специальную обработку аргументов -r, -f, -q . Сделай функцию pwd,
  если её ещё нет. К функции set_trace добавь противоположную unset_trace. Проверь, чтобы у grep_r и grep_p были инвертирующие фильтры grep_r_v и grep_p_v. Может, сделать sed_r и
  sed_r_z? Проверь, чтобы head и tail принимали отрицательные аргументы и вели приэтом себя так же, как и gnu head и tail - а именно исключали соответственно с конца и с начала
  строки из вывода. Переименуй (если не будет мешать никому) to_stdout() в stdout(), to_stderr() в stderr(), и добавь функции с аргументами: stdout(f string, flag append=true) ==
  write_to_file(f) выводить stdout в файл f (аппенд, но если append=false, то перезаписать), и stderr(f string) == write_to_file(f) выводит stderr соотвественно (так же с флагом
  append). Переименуй cat_file в cat(f string), а cat_stdin() сделай вариацией cat() без аргументов. К ls(...) добавь ls_l(...), чтобы выводило в формате `ls -l`.

